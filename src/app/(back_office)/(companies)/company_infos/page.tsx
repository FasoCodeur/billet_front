import Header from '@/components/header';
import React from 'react';

const Page = () => {
    return (
        <div>
            <Header title="company info" total ={0} />
        </div>
    );
};

export default Page;